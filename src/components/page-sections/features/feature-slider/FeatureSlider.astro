---
import CustomSection from "@builders/custom-section/CustomSection.astro";
import FeatureSliderItem from "@features/feature-slider/FeatureSliderItem.astro";
import Carousel from "@wrappers/carousel/Carousel.astro";

type SlideProps = Record<string, unknown>;

const {
  id = "",
  colorScheme = "inherit",
  backgroundColor,
  paddingVertical = "4xl",
  rounded = false,
  class: className,
  slides = [],
  _uuid,
  _component,
  ...htmlAttributes
} = Astro.props;
---

<CustomSection
  class:list={["feature-slider", className]}
  maxContentWidth="xl"
  paddingHorizontal="lg"
  paddingVertical={paddingVertical}
  colorScheme={colorScheme}
  backgroundColor={backgroundColor}
  rounded={rounded}
  id={id}
  data-rosey-ns={_uuid}
  {...htmlAttributes}
>
  <Carousel
    label="Feature slider"
    slideWidthPercent={100}
    minSlideWidth={0}
    autoPlay={false}
    autoScroll={false}
    data-children-prop="slides"
  >
    {
      slides.map((slide: SlideProps, index: number) => (
        <FeatureSliderItem
          aria-label={`Slide ${index + 1} of ${slides.length}`}
          data-editable="array-item"
          data-component="page-sections/features/feature-slider/feature-slider-item"
          data-id={slide._uuid ?? "page-sections/features/feature-slider/feature-slider-item"}
          {...slide}
        />
      ))
    }
  </Carousel>
</CustomSection>
