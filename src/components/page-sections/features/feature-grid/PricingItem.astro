---
import Button from "@core-elements/button/Button.astro";
import Heading from "@core-elements/heading/Heading.astro";
import List from "@core-elements/list/List.astro";
import Text from "@core-elements/text/Text.astro";
import BentoBoxGridItem from "@wrappers/bento-box-grid/BentoBoxGridItem.astro";
import Card from "@wrappers/card/Card.astro";

const {
  title = "",
  price = {
    prefix: "",
    amount: "",
    suffix: "",
  },
  subtext = "",
  list = [],
  button = {},
  columnSpan,
  rowSpan,
  class: className,
  _uuid,
  _component,
  editable = false,
  ...htmlAttributes
} = Astro.props;
---

<BentoBoxGridItem
  columnSpan={columnSpan}
  rowSpan={rowSpan}
  data-prop-columnSpan="columnSpan"
  data-prop-rowSpan="rowSpan"
  data-rosey-ns={_uuid}
  {...htmlAttributes}
>
  <Card
    class:list={["pricing-item", className]}
    paddingHorizontal="lg"
    paddingVertical="lg"
    rounded
    border
    backgroundColor="base"
  >
    <Heading
      class="feature-title"
      level="h3"
      size="lg"
      alignX="start"
      data-prop="title"
    >
      {title}
    </Heading>
    <Heading
      class="feature-price"
      level="h4"
      size="xl"
      alignX="start"
      editable={false}
    >
      {/* Keys are written out rather than derived from data-prop, because a dot in a
          Rosey key is read as a nested path when the connector saves an edit. The
          amount is a number and gets no key at all. */}
      {
        price?.prefix && (
          <span
            data-editable="text"
            data-prop="price.prefix"
            data-rosey="price_prefix"
          >
            {price?.prefix}
          </span>
        )
      }
      {
        price?.amount && (
          <span
            data-editable="text"
            data-prop="price.amount"
          >
            {price?.amount}
          </span>
        )
      }
      {
        price?.suffix && (
          <span
            data-editable="text"
            data-prop="price.suffix"
            data-rosey="price_suffix"
            class="feature-price-suffix"
          >
            {price?.suffix}
          </span>
        )
      }
    </Heading>
    <Text
      class="feature-subtext"
      alignX="start"
      data-prop="subtext"
      text={subtext}
    />
    <List
      {...list}
      data-children-prop="list.items"
    />
    <Button
      {...button}
      data-prop="button.text"
    />
  </Card>
</BentoBoxGridItem>

<style is:global>
  @layer page-sections {
    .pricing-item {
      .card-content {
        display: flex;
        flex-direction: column;
        gap: var(--spacing-md);
      }

      .feature-price .heading-text {
        display: flex;
      }
      .feature-price-suffix {
        font-size: var(--font-size-heading-md);
        line-height: 1.75;
        align-self: flex-end;
      }
      .feature-title,
      .feature-price,
      .feature-subtext,
      .feature-subtext p {
        font-weight: var(--font-weight-bold);
        margin: 0;
      }
      .list {
        margin: 0;
      }
      .button-inner {
        align-self: flex-start;
      }
    }
  }
</style>
