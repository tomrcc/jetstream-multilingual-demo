---
import FeatureCarouselCard from "@features/feature-carousel-grid/FeatureCarouselCard.astro";
import BentoBoxGrid from "@wrappers/bento-box-grid/BentoBoxGrid.astro";
import CarouselSlide from "@wrappers/carousel/CarouselSlide.astro";

const {
  cards = [],
  colorScheme = "inherit",
  backgroundColor,
  class: className,
  _uuid,
  _component,
  editable = false,
  ...htmlAttributes
} = Astro.props;
---

<CarouselSlide
  data-rosey-ns={_uuid}
  {...htmlAttributes}>
  <BentoBoxGrid
    class:list={["feature-carousel-slide-grid", className]}
    gap="xs"
    data-children-prop="cards"
  >
    {
      cards.map((card: any) =>
        card.decorative ? (
          <FeatureCarouselCard
            columnSpan={card.columnSpan}
            rowSpan={card.rowSpan}
            backgroundColor={card.style}
            border={false}
            data-editable="array-item"
            data-component="page-sections/features/feature-carousel-grid/feature-carousel-card"
            data-id={card._uuid ?? "page-sections/features/feature-carousel-grid/feature-carousel-card"}
          />
        ) : (
          <FeatureCarouselCard
            {...card}
            backgroundColor="base"
            border={true}
            data-editable="array-item"
            data-component="page-sections/features/feature-carousel-grid/feature-carousel-card"
            data-id={card._uuid ?? "page-sections/features/feature-carousel-grid/feature-carousel-card"}
          />
        )
      )
    }
  </BentoBoxGrid>
</CarouselSlide>

<style is:global>
  @layer page-sections {
    .feature-carousel-slide-grid {
      width: 100%;
      padding: var(--spacing-sm);
    }
  }
</style>
