---
const {
  value = "",
  label = "",
  selected = false,
  disabled = false,
  "data-rosey": roseyId,
  _component,
  ...htmlAttributes
} = Astro.props;
---

<option
  value={value}
  selected={selected}
  disabled={disabled}
  data-rosey={roseyId}
  {...htmlAttributes}
>
  {label}
  <slot />
</option>
